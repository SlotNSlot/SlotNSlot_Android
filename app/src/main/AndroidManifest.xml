<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.slotnslot.slotnslot">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PROFILE" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service android:name=".service.GethNodeService" />

        <activity
            android:name=".activities.LandingPageActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SlotMainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.MakeSlotActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".activities.SlotGameActivity"></activity>
        <activity
            android:name=".activities.SignInUpActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.MyPageActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- for test -->
        <activity
            android:name=".activities.example.MainActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".activities.example.AccountActivity" />
        <activity android:name=".activities.example.ContractActivity" />
        <activity android:name=".activities.example.PlayExampleActivity" />
        <activity android:name=".activities.example.FilterExampleActivity" />
        <activity android:name=".activities.example.PendingExampleActivity"></activity>
    </application>

</manifest>